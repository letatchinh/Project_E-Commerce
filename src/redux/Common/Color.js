const initvalue = {
  colorPrimary : "#ee4d2d"
};
const commonReducer = (state = initvalue, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default commonReducer;
